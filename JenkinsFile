pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                // sh '''
                // sudo npm install
                // npm run build
                // '''
                echo 'default workspace'
                // sh "pwd"
            }
        }
    }
}
