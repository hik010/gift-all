pipeline {
    agent {
        node {
        label 'ubuntu'
        customWorkspace '/home/ubuntu/apps/gift-app'
        }
  }
    stages {
        stage('Hello') {
            steps {
                // sh '''
                // sudo npm install
                // npm run build
                // '''
                echo 'default workspace'
                sh "pwd"
            }
        }
    }
}
